name: Java CI

on:
  push:
    branches:
      - master  # Triggers the workflow on pushes to the master branch


jobs:
  build:
    runs-on: ubuntu-latest  # Use the latest Ubuntu runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v2  # Check out the repository code

      - name: Set up JDK 23
        uses: actions/setup-java@v2  # Use the correct version of the action
        with:
          java-version: '23'  # Specify the Java version you want to use
          distribution: 'temurin'  # Specify the distribution (e.g., 'temurin', 'adopt', etc.)

      - name: Run unit tests
        run: |
          cd HW1/meal-booking-backend  
          sudo mvn clean test
      - name: Run integration tests
        run: |
          cd HW1/meal-booking-backend
          sudo mvn clean failsafe:integration-test verify sonar:sonar -Dsonar.projectKey=HW1   -Dsonar.projectName='HW1'   -Dsonar.host.url=http://localhost:9000   -Dsonar.token=sqp_337d43d121b527eb7c192ede54a6d6c027c7c38d

